# Open questions register

Every unresolved item, with where it gets decided. Status values:
`scheduled-for-Gn` (a grill session owns it), `carried` (deliberately open,
with reason), `resolved-by-ADR-nnnn`. A question may not be silently dropped.

| ID | Question | Status | Notes |
|---|---|---|---|
| OQ-1 | Staged rollout across two delivery paths. Is a Cohort expressible as git state (directory/branch + selector)? Who computes membership deterministically? What does a rollout look like as PRs? What failure signal halts it, and does the foreign population get any of this? | resolved-by-ADR-0029 | Hypothesis confirmed. Rollout = opt-in authored object; Tier temporarily dual-bound, both artefacts at head. Cohort = enumerated/attribute/fractional subdivision of one Tier's population; membership a pure function at serve time. Platform-proposed advance PRs; passive halt + threshold abort PR; Foreign reads everything (advisory membership × commit stamp), blocks nothing. |
| OQ-2 | First boot, no cache: a healthy collector running a `nop` pipeline. What does the platform show? Is "expected but never seen" a conformance outcome or a separate finding class (it has no collector to attach to)? | resolved-by-ADR-0030 | Matched first boot is the ordinary serve path. Unmatched served collectors get the root-team-owned Unmatched artefact (stamped, self-telemetry, no data pipelines). `never_seen` is a separate Tier-attached finding class, neutral in v1; cardinality teeth arrive with OQ-5 in G5. |
| OQ-3 | A discovered collector matching no selector is the ungoverned quadrant. Does it appear in the estate view, and how, without reading as failure? | resolved-by-ADR-0031 | G2 early verdicts ratified verbatim (onboard CTA + denominator exclusion; quarantine as rendered pattern), sharpened into two named referents — ungoverned collector (population-level) vs ungoverned data (signal-level) — plus the Unmatched artefact (ADR-0030) as the served leg. CTA authoring flow delivered as the Claim flow, ADR-0042 §6. |
| OQ-4 | Schema-conformance as a requirement kind: how Weaver vocabulary lands in the requirement model; four-level `requirement_level` adoption; `AttributeNames` fidelity on index-scoped backends; and the real decision — is a collection-time `live-check` tap in-product, recommended-but-external, or refused? It collides with REQ-002. | resolved-by-ADR-0034 | Schema Registry = second Catalogue-pattern substrate; `schema_conformance` = pinned reference + scope, two placements (`landed`/`live`). Violations flip to `misconfigured`; `conditionally_required` honestly demoted; no eighth outcome. Seam widens by `AttributeNames`/`DistinctValues`/grouping key, service-scoped by contract, `Known: false` on degradation, union as screening fast path. Tap ruling: in-product as pattern-and-reading, external as runtime, refused as platform component; teed sampled branch, findings home via `--emit-otlp-logs`. Findings Service-owned, registry-derived remediation, enrichment suggestion only. |
| OQ-5 | Cardinality source per substrate: a selector says what shape should exist, not how many. Kubernetes node count answers; VM inventory maybe; bare metal possibly nothing. | resolved-by-ADR-0035 | New narrow seam `InventoryProvider` (count + as-of, `Known: false` allowed; `Kubernetes` first-party, floats with the autoscaler). Expectations are floors (`min_expected`), never equalities; derived > declared > absent (absent = no teeth, count never invented); shortfalls persistence-dampened. `never_seen` escalates only under a floor; new sibling `under_populated`; both delivery-kind, Tier-owned. Aged neutral `never_seen` = stale-config deletion signal. |
| OQ-6 | `EstateProvider` minimum-populated-set rule, stated so it can be checked: which fields must a conforming implementation populate, covering freshness not only presence? | resolved-by-ADR-0036 | Static capability declaration (incapable ≠ silent); minimum set = identity attributes + `as_of` on every reading + capable readings populated-or-`Known:false` + structural invariants. Staleness computed by the platform, demotes to `Known:false` at evaluation (surfaces may show last-known-plus-age). Rule embodied as a shipped conformance test kit; same kit pattern for `InventoryProvider`. |
| OQ-7 | Multi-tenancy and the auth model. | resolved-by-ADR-0019 | Pluggable authn (OIDC/SAML/basic); ownership-derived authz; air-gap first-class; read isolation = instance per domain. |
| OQ-8 | Tenancy-to-git mapping: repo-per-team vs path-per-team for rendered artefacts. | resolved-by-ADR-0018 | One estate monorepo, path-per-team, generated code-ownership. G3 designs the layout inside it. |
| OQ-9 | Component Catalogue mechanics: `metadata.yaml` coverage/stability, versioning against collector releases, whether the Catalogue is a seam. | resolved-by-ADR-0020/0021/0022/0023 | Substrate + atomic versioning + import pipeline (0020); allow-list inheritance and Grants (0021); evaluator-as-a-service, one hard rule (0022); Environment axis + floors (0023). |
| OQ-10 | Blueprint versioning and upgrade story; `satisfies` linkage to `library_drift`; phase-collision rules. | resolved-by-ADR-0024/0025/0026/0028 | Schema v1: domain document, per-signal lanes, everything a Component, integer versions (0024). Tier = rendering/binding unit; per-Environment binding via sibling Tiers (0025). Pinned refs + opt-in tracking; `library_drift` = passes-claimed-fails-current, unified across Requirement/Component facets (0026). Phase-collision: phases dropped — nothing to collide; ordering findings cover it (0024). Render-in-PR + fail-closed CI default with retry (0028). |
| OQ-16 | Do Requirements and delivery expectations vary per Environment (e.g. completeness required in production only)? Floors already do (per Service Class × Environment, G2). | resolved-by-ADR-0033 | Evaluation unit is `(Service, Environment, Requirement)`; verdicts never blend across envs; `production` is the default lens. Requirements gain an optional `environments` applicability list (absent = all envs), strict-loaded, with a never-matches authoring finding. Delivery needs no separate mechanism — env inherited from the Tier; per-env severity policy deferred as tightening. |
| OQ-11 | Scoring beyond binary: per-requirement binary aggregated to a per-service ratio — and a per-team roll-up. | resolved-by-ADR-0017 | Ratio-plus-worst per finding kind, waivers always visible, no blended number at any level. |
| OQ-12 | Evaluator cardinality and cost: attribute coverage across a large estate; correct semconv checks are per span/metric name, multiplying aggregations. | carried | Revisit when scale data exists; note in Phase 1 design. G6 addition: expectation derivation is recomputed per evaluation (pure function, loseable memoisation only — ADR-0038) and metering is computed on read (ADR-0040); both inherit this ceiling. Mitigating structure: cardinality follows authored objects (Tiers × components × signals), never collector count. |
| OQ-13 | Project name and visual identity. | resolved-by-ADR-0047 | Both halves closed. The name settled in `branding/naming.md` and is the stamp the served artefacts carry (ADR-0013). The identity settled in ADR-0047: our own token layer rather than a framework; dark-first with a complete light twin resolved in three states; Atkinson Hyperlegible and JetBrains Mono, self-hosted; colour spent on meaning, with hue never load-bearing and a signal colour never appearing without its lane name; state marks drawn by us because no pack draws `pending_settle` the way this product means it. The accessibility floors are numbers and are checked in CI by `console/tools/check-palette.mjs`, not asserted in a document. ADR-0048 followed on the console primitive layer the pass exposed. Publishing was the thing this blocked, and it is unblocked. |
| OQ-14 | UI surface inventory, navigation, canvas interaction model, tech stack. | resolved-by-ADR-0042/0043/0044/0045 | Activity-first console: four Workspaces (Estate / Topology / Compose / Catalogue & Governance) holding the eleven verdict surfaces as view-switchers over one model, with global jump-to-object search (0042). Shelf: my-teams default, team-subtree sections × env rows production-first, worst-severity ordering from face fields, neutrals visible at the tail, healthy sections collapse to a line — cards never collapse. Linking: universal card panel summoned in place, who-acts chips deep-link to the fixing surface (inspect stays, action travels), counts open the flat list, every state URL-addressable. Environment lens = emphasis + evaluation context, never a filter; "why?" = provenance popover with trace action. Claim flow delivers the OQ-3 CTA (herd-first, generalise-never-enumerate, PR exit). Composer ratified per P1 (0043). One canvas engine, two vocabularies, derived-never-drawn edges, constrained drag, cosmetic simulate (0044). Stack: TypeScript/React/Vite, xyflow + pure engine with custom-SVG escape hatch, TanStack Query/Router, Radix + tokens, zero-CDN enforced in CI (0045). Sole non-git state: the per-user Presentation store. |
| OQ-15 | Per-collector cache semantics in the server path: is anything cached, where, for how long — cache, not record. Layer-1 digest must be confirmed loseable. | resolved-by-ADR-0032 | Closed three-item list: repo snapshot (+selector index, fetch-interval staleness), in-memory per-connection layer-1 digest (confirmed loseable — one extra parse), nothing else. HA coordination-free (ref CAS + deterministic branch names). Local bare repo = full standalone instance: git-the-tool, never git-the-service. |
| OQ-17 | Cross-instance verdict federation: a central instance receiving roll-ups (verdicts, not content) from instances whose operators the sensitive domain distrusts. | carried | Raised in G3's satellite-repo discussion (ADR-0027). Satellites cover content-privacy on a shared instance; federation is the answer when the threat model includes the platform's own operators. Post-v1; revisit if a real adopter needs it. |
| OQ-18 | Component behaviour-model layer for enrichment claims: v1 derives only what the config literally states, so `k8sattributes` ⇒ `k8s.*` yields no claim. A curated, Catalogue-resident, versioned model of what components emit would widen enrichment claims. Where does it live, who curates it, how does it version against collector releases? | carried | Named as the extension seam in ADR-0038 §2 — deliberately refused in v1 (a false expectation-red would poison trust in the band). Revisit post-v1 when literal-only claims prove too narrow in practice. |
