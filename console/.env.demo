# Demo mode (issue #50): the console reads a build-time snapshot of the
# platform API instead of calling a server. `npm run build:demo` selects
# this file; every other build leaves the flag unset and the live client in
# place.
VITE_DEMO=1
